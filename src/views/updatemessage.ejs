<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MembersOnly! | Update</title>
  </head>
  <body>
    <h1>Update an existing message</h1>
    <%- include('./partials/err') %>

    <form action="/message/update/<%= messageToUpdate.id || message.id %>" method="post">
      <label for="title">Title</label>
      <input type="text" name="title" id="title" value="<%= messageToUpdate.title || message.title %>" required minlength="1" />
      <label for="message">Message</label>
      <textarea name="message" id="message"><%= messageToUpdate.message || message.message %></textarea>
      <input type="submit" value="Submit" />
    </form>
  </body>
</html>
